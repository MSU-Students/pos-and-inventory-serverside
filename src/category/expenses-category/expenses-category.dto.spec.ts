import { ExpensesCategoryDto } from './expenses-category.dto';

describe('ExpensesCategoryDto', () => {
  it('should be defined', () => {
    expect(new ExpensesCategoryDto()).toBeDefined();
  });
});
